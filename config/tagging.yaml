data:
 auxiliary_scalars: all
 canonicalize: null
 canonicalize_spurions: true
 beam_reference: all
 two_beams: true
 add_time_reference: true
 mass_reg: 5e-3  # only used in LLoCa networks
 spurion_scale: 1
 units: 1  # only used in LGATr/LGATrSlim/LorentzNet/PELICAN
 max_particles: null
 momentum_float64: true

evaluation:
 save_roc: false
 eval_set: [train, test, val]

plotting:
 loss: true
 roc: false
 score: false

defaults:
 - model: tag_transformer
 - default
 - _self_
