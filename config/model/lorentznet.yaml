# this config does not support variable-size;
# it is only meant to be used for atlastop uncertainty studies
_target_: experiments.tagging.wrappers.LorentzNetWrapper
out_channels: null
units: null

net:
 _target_: experiments.baselines.lorentznet.LorentzNet
 _partial_: true
 size: -2  # set lr when using with training=jc

 n_scalar: null
 n_class: null
 n_hidden: 72
 n_layers: 6
 c_weight: 5e-3
 dropout: 0.2

defaults:
 - framesnet: identity
